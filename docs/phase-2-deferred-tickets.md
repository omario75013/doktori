# Phase 2 — Deferred tickets

Discovered during Phase 2 #21 Redis cache implementation (2026-05-05). Tracked here so they don't get lost.

## ~~DOKTORI-MISSING-APPT-VALIDATION~~ — RESOLVED 2026-05-06

The original ticket diagnosis was wrong. After reading the route code and its callers (`template-lookup.tsx`), the route is **intentionally** flexible: appointmentId is optional, and absence triggers a fall-back check on doctor-patient relationship existence. The `template-lookup.tsx` UI relies on this dual mode.

The bad test was rewritten to verify the actual design contract:
- No appointmentId + doctor has appointment with patient → 200 OK
- No appointmentId + doctor has NO appointment with patient → 403

Resolved in commit covering both the rewrite and this update.

---

## ~~DOKTORI-FLAKY-TESTS~~ — RESOLVED 2026-05-06

Both flaky tests fixed:

1. **`templates.test.ts > double audit B5`** — added `beforeAll` hook seeding the SUPER_ADMIN row in `admin_users` table with `onConflictDoNothing`. FK constraint now satisfied; audit log inserts succeed.
2. **`template-context.test.ts > 400 missing appointmentId returns 400`** — discovered to be a wrong test, not a real bug. Rewrote into two correct tests matching the actual route design (200 with relation, 403 without). See `DOKTORI-MISSING-APPT-VALIDATION` resolution.

CI is green with 0 skipped tests.

---

## ~~DOKTORI-1P-DEBT~~ — RESOLVED 2026-05-06

All 8 secrets bootstrapped to 1P (`Doktori Prod - <KEY>` items in vault `Dartank-Infra`). `.env.tpl` aligned with prod literal values. `scripts/deploy.sh` runs `op inject` + docker compose build/up + healthcheck. GH Actions deploy workflow switched to invoke this script.

Verified end-to-end: `op inject .env.tpl > .env` produces byte-equivalent output to the previous hand-managed `.env`. New deploys are now fully reproducible from 1P, no manual SSH for secret rotation.

5 R2 fields are referenced from the existing `Ecommerce Prod - R2_*` items (shared bucket `dartank-images`).

Closed in commits: 96c5c1e (lib), 889e686 (env.tpl alignment), 1dd8dd2 (workflow switch), and the bootstrap session 2026-05-06 (1P items created).

---

## ~~DOKTORI-1P-DEBT~~ (original ticket below for history)

**Discovered:** 2026-05-05 during Phase 2 #21 Tasks 10-11 (REDIS_PASSWORD provisioning).

**Symptom:** `/opt/doktori/.env.tpl` references `op://Dartank-Infra/Doktori Prod/FIELD` paths but no item titled `Doktori Prod` exists in the vault. The convention used elsewhere in the same vault is **one item per secret** (e.g. `Doktori Prod - DEEPL_API_KEY`, `B2B Prod - STRIPE_SECRET_KEY`). The current `/opt/doktori/.env` is therefore NOT generated by `op inject` — it's a static file with hardcoded values written manually.

## Audit of `/opt/doktori/.env` (2026-05-05)

24 environment variables. Categorised below.

### Sensitive — must be in 1Password (10 unique secrets, after dedup)

| Variable | Currently in 1P? | Action |
|---|---|---|
| `POSTGRES_PASSWORD` | ❌ | Create `Doktori Prod - POSTGRES_PASSWORD` |
| `DATABASE_URL` | ❌ | Create `Doktori Prod - DATABASE_URL` (contains POSTGRES_PASSWORD inline — could be templated from POSTGRES_PASSWORD instead, but storing whole URL is simpler) |
| `MEILISEARCH_KEY` | ❌ | Create `Doktori Prod - MEILISEARCH_KEY` |
| `NEXTAUTH_SECRET` | ❌ | Create `Doktori Prod - NEXTAUTH_SECRET` |
| `CRON_SECRET` | ❌ | Create `Doktori Prod - CRON_SECRET` |
| `SOCKETIO_BROADCAST_SECRET` | ❌ | Create `Doktori Prod - SOCKETIO_BROADCAST_SECRET` |
| `OPENROUTER_API_KEY` | ❌ in Doktori, but `Monitor Prod - OPENROUTER_API_KEY` exists | Either reference Monitor's item or create a Doktori-specific one (recommend: separate, in case Monitor rotates first). Create `Doktori Prod - OPENROUTER_API_KEY` |
| `REDIS_PASSWORD` | ❌ | Create `Doktori Prod - REDIS_PASSWORD` (value already in `/opt/doktori/.env`, generated 2026-05-05) |

### Shared with Ecommerce — already in 1P (5 R2 fields)

`R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_ACCOUNT_ID`, `R2_BUCKET_NAME`, `R2_PUBLIC_URL` — Doktori and Ecommerce use the **same** R2 bucket (`dartank-images`). Verified that `Ecommerce Prod - R2_BUCKET_NAME/password` returns the same value as `/opt/doktori/.env`'s `R2_BUCKET_NAME=dartank-images`. The new Doktori `.env.tpl` should reference `op://Dartank-Infra/Ecommerce Prod - R2_*` directly — DO NOT duplicate. (If a Doktori-specific R2 bucket is ever created, switch to Doktori-prefixed items.)

### Already in 1P — Doktori-specific

- `DEEPL_API_KEY` — `op://Dartank-Infra/Doktori Prod - DEEPL_API_KEY/password` (already used in older Doktori code)

### Non-secret config — keep as plain literals in `.env.tpl` (no 1P needed)

| Variable | Value |
|---|---|
| `NEXTAUTH_URL` | `https://doktori.tn` |
| `MEILISEARCH_URL` | `http://meilisearch-doktori:7700` |
| `NEXT_PUBLIC_SOCKETIO_URL` | `https://doktori.dartank.com` (or `https://sos.doktori.tn` — verify) |
| `SOCKETIO_INTERNAL_URL` | `http://localhost:3010` |
| `AUTH_TRUST_HOST` | `true` |
| `OPENROUTER_MODEL` | model name (e.g. `moonshotai/kimi-k2-0905`) |
| `SUPER_ADMIN_EMAILS` | comma-separated list of admin emails |

## Bootstrap procedure (you, from 1Password desktop app)

The prod service account token is read-only on the vault — items must be created via the desktop 1Password app. Items to create manually:

| Item | Title | Tags | Category | Field `password` value source |
|---|---|---|---|---|
| 1 | `Doktori Prod - POSTGRES_PASSWORD` | `doktori,postgres` | Password | `ssh root@157.90.152.204 'grep ^POSTGRES_PASSWORD /opt/doktori/.env \| cut -d= -f2'` |
| 2 | `Doktori Prod - DATABASE_URL` | `doktori,postgres` | Password | `ssh root@157.90.152.204 'grep ^DATABASE_URL /opt/doktori/.env \| cut -d= -f2-'` |
| 3 | `Doktori Prod - MEILISEARCH_KEY` | `doktori,meilisearch` | Password | `ssh root@157.90.152.204 'grep ^MEILISEARCH_KEY /opt/doktori/.env \| cut -d= -f2'` |
| 4 | `Doktori Prod - NEXTAUTH_SECRET` | `doktori,auth` | Password | `ssh root@157.90.152.204 'grep ^NEXTAUTH_SECRET /opt/doktori/.env \| head -1 \| cut -d= -f2'` |
| 5 | `Doktori Prod - CRON_SECRET` | `doktori,cron` | Password | `ssh root@157.90.152.204 'grep ^CRON_SECRET /opt/doktori/.env \| head -1 \| cut -d= -f2'` |
| 6 | `Doktori Prod - SOCKETIO_BROADCAST_SECRET` | `doktori,socketio` | Password | `ssh root@157.90.152.204 'grep ^SOCKETIO_BROADCAST_SECRET /opt/doktori/.env \| head -1 \| cut -d= -f2'` |
| 7 | `Doktori Prod - OPENROUTER_API_KEY` | `doktori,ai` | Password | `ssh root@157.90.152.204 'grep ^OPENROUTER_API_KEY /opt/doktori/.env \| cut -d= -f2'` |
| 8 | `Doktori Prod - REDIS_PASSWORD` | `doktori,redis` | Password | `ssh root@157.90.152.204 'grep ^REDIS_PASSWORD /opt/doktori/.env \| cut -d= -f2'` |

**~5 minutes for you in the 1Password app.** All values come straight from prod `.env`.

## After bootstrap — implementation steps (me, in a follow-up session)

1. Replace `/opt/doktori/.env.tpl` with the new version (draft below).
2. Test locally: `op inject -i .env.tpl -o .env.test`, diff against current `.env` (should produce identical content).
3. Sync new `.env.tpl` to prod, run `op inject` to materialise `.env`.
4. Document deploy procedure: rsync + `op inject` + docker compose build/up.
5. Add `.env` to a deploy-time hook (`ExecStartPre` style for systemd, but Doktori is docker compose — wrap into a deploy script `scripts/deploy.sh` that does `op inject` then `docker compose up`).
6. Document in `docs/operations/secrets.md` (new file).

## Draft of new `.env.tpl` (after bootstrap)

```
# === SECRETS — resolved by op inject ===
POSTGRES_PASSWORD=op://Dartank-Infra/Doktori Prod - POSTGRES_PASSWORD/password
DATABASE_URL=op://Dartank-Infra/Doktori Prod - DATABASE_URL/password
MEILISEARCH_KEY=op://Dartank-Infra/Doktori Prod - MEILISEARCH_KEY/password
NEXTAUTH_SECRET=op://Dartank-Infra/Doktori Prod - NEXTAUTH_SECRET/password
CRON_SECRET=op://Dartank-Infra/Doktori Prod - CRON_SECRET/password
SOCKETIO_BROADCAST_SECRET=op://Dartank-Infra/Doktori Prod - SOCKETIO_BROADCAST_SECRET/password
OPENROUTER_API_KEY=op://Dartank-Infra/Doktori Prod - OPENROUTER_API_KEY/password
REDIS_PASSWORD=op://Dartank-Infra/Doktori Prod - REDIS_PASSWORD/password
DEEPL_API_KEY=op://Dartank-Infra/Doktori Prod - DEEPL_API_KEY/password

# === SHARED with Ecommerce — same R2 bucket ===
R2_ACCOUNT_ID=op://Dartank-Infra/Ecommerce Prod - R2_ACCOUNT_ID/password
R2_ACCESS_KEY_ID=op://Dartank-Infra/Ecommerce Prod - R2_ACCESS_KEY_ID/password
R2_SECRET_ACCESS_KEY=op://Dartank-Infra/Ecommerce Prod - R2_SECRET_ACCESS_KEY/password
R2_BUCKET_NAME=op://Dartank-Infra/Ecommerce Prod - R2_BUCKET_NAME/password
R2_PUBLIC_URL=op://Dartank-Infra/Ecommerce Prod - R2_PUBLIC_URL/password

# === Non-secret config — plain literals ===
NEXTAUTH_URL=https://doktori.tn
MEILISEARCH_URL=http://meilisearch-doktori:7700
NEXT_PUBLIC_SOCKETIO_URL=https://doktori.dartank.com
SOCKETIO_INTERNAL_URL=http://localhost:3010
AUTH_TRUST_HOST=true
OPENROUTER_MODEL=moonshotai/kimi-k2-0905
SUPER_ADMIN_EMAILS=admin@dartank.com
```

**Estimated effort after bootstrap:** ~1h (rsync, op inject, verify, document, redeploy).

## Future-proofing for Phase 2

After this debt is cleared, adding new secrets follows a clean pattern:
- `KONNECT_API_KEY` (#2 Paiement) → `Doktori Prod - KONNECT_API_KEY` in 1P + line in `.env.tpl`
- Deploy procedure remains identical (`op inject` reads it automatically)

## Verification of Phase 2 credential availability (audit 2026-05-05)

Cross-reference of Phase 2 needs vs vault contents:

| Need | Status | Notes |
|---|---|---|
| **Cloudflare account** (#20 CDN) | ✅ exists | Confirmed via `R2_ACCOUNT_ID=07ca38ff7938f44398b062456cc6da23`. Adding `doktori.tn` as a zone in this same account = 5 min in CF dashboard. |
| **Cloudflare API Token** (#20 CDN) | ❌ not in vault | You create it in CF dashboard with scope `Zone:DNS:Edit` for `doktori.tn`, store as `Cloudflare - API Token (DNS edit)` |
| **DNS registrar credentials** for doktori.tn | ❌ not in vault | Need to know where the domain is hosted (OVH ? Tunisie ?). One-time access for NS change to Cloudflare. |
| **Stripe** (#2 Paiement) | ✅ partially | `Ecommerce Prod - STRIPE_SECRET_KEY` exists. Decide: reuse same Stripe account or create Doktori sub-account. |
| **Konnect** (#2 Paiement Tunisie local) | ❌ not in vault | Account creation needed (RNE 1625867B per spec). |
| **Kimi** (#9 Coach IA) | ✅ via OpenRouter | `Monitor Prod - OPENROUTER_API_KEY` exposes `moonshotai/kimi-k2-0905` at $0.40/M input / $2.00/M output. Doktori `.env` already has its own `OPENROUTER_API_KEY` and `OPENROUTER_MODEL`. **No need for direct `MOONSHOT_API_KEY`**. |
| **R2 storage** | ✅ shared with Ecommerce | Same bucket `dartank-images`, same credentials. |
